<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.osu.recipe_app">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.TodaysPick.TodaysPickActivity"></activity>
        <activity android:name=".ui.MyRecipes.MyRecipeActivity"></activity>
        <activity android:name=".ui.Favorites.FavoritesActivity"></activity>
        <activity android:name=".ui.Timer.TimerActivity"></activity>
        <activity android:name=".ui.FindStore.FindStoreActivity"></activity>
        <activity android:name=".ui.MyAccount.AccountNewActivity"></activity>
        <activity android:name=".ui.MyAccount.AccountLoginActivity"></activity>

    </application>

</manifest>